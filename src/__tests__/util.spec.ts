import { describe, expect, it } from "vitest";
import { util } from "../util.js";

describe("sum function", () => {
  it("should add two positive numbers correctly", () => {
    expect(util()).toBe("Hello");
  });
});
